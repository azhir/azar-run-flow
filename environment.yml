name: azar-run-flow

channels:
  - conda-forge

dependencies:
  - python=3.11
  - pip

  # Add Conda-specific dependencies here when needed.
  # Examples:
  # - fenics
  # - petsc
  # - hdf5

  - pip:
      - -e .